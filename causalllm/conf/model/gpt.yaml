# conf/model/qwen.yaml
name: "gpt-4o-mini"
new_model: ${model.name}-${dataset.name}${bool2name:${experiment.enable_cot}, "-cot"}}${bool2name:${experiment.enable_fewshot}, "-fs"}}
trust_remote_code: true