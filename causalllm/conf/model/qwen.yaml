# conf/model/qwen.yaml
name: "Qwen/Qwen2.5-3B-Instruct"
new_model: SFT/QWen-${dataset.name}-R${experiment.reasoning}-DP${dataset.percent_of_train_dataset}-s${training.max_steps}-Abs${dataset.anonymize}-lr${training.learning_rate}
trust_remote_code: true